apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "canary-app-helm.fullname" . }}
  labels:
    {{- include "canary-app-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  revisionHistoryLimit: 0  # Do not retain any history of old ReplicaSets
  selector:
    matchLabels:
      app: {{ include "canary-app-helm.fullname" . }}
  strategy:
    canary:
      canaryService: {{ include "canary-app-helm.fullname" . }}-canary
      stableService: {{ include "canary-app-helm.fullname" . }}-stable
      trafficRouting:
        nginx:
          stableIngress: {{ include "canary-app-helm.fullname" . }}-ingress
          canaryIngress: {{ include "canary-app-helm.fullname" . }}-ingress-canary
      steps:
      - setWeight: 0
      - pause: {}  # Wait for tests to complete
      - setWeight: 10       # Shift 10% of traffic to canary
      - pause: {}           # Pause to test new version
      - setWeight: 50       # Shift 50% of traffic to canary
      - pause: {}           # Pause to test new version
      - setWeight: 100      # Shift 100% of traffic to canary (promote fully)
  template:
    metadata:
      labels:
        app: {{ include "canary-app-helm.fullname" . }}
        {{- include "canary-app-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: demo-app
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        ports:
        - containerPort: {{ .Values.service.port }}
